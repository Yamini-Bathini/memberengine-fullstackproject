{
  "name": "memberengine-backend",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
  "backend": "nodemon server.js",
  "frontend": "live-server .. --port=3000",
  "dev": "concurrently --kill-others-on-fail -n BACKEND,FRONTEND -c blue,green \"npm run backend\" \"npm run frontend\""
},
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.3",
    "mysql2": "^3.9.7"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "live-server": "^1.2.2",
    "nodemon": "^3.0.2"
  }
}
